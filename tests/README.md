# Testes

Use esta pasta para organizar testes automatizados, dados de fixtures e utilidades de mocking. Adote a mesma estrutura do código-fonte (`unit`, `integration`, `e2e`, etc.) para facilitar a manutenção.
